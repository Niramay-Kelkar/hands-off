{
  "capability_id": "meridian_funds_transfer",
  "description": "Sign on to MERIDIAN CORE, transfer funds between two shares for a member, and post the transaction for real.",
  "version": 1,
  "risk_class": "mutating",
  "expected_outcomes": [
    {
      "code": "INVALID_CREDENTIALS",
      "description": "The operator ID/password/branch combination was rejected by the sign-on form",
      "detection": {
        "type": "text_present",
        "value": "Invalid operator ID or password.",
        "scope": "page"
      }
    },
    {
      "code": "INSUFFICIENT_FUNDS",
      "description": "The requested amount exceeds the source share's available balance",
      "detection": {
        "type": "text_present",
        "value": "Insufficient available balance in the source share.",
        "scope": "page"
      }
    },
    {
      "code": "SHARE_ON_HOLD",
      "description": "The source share is on HOLD and cannot be debited -- detected from the post-attempt server response, not the member record page's OPEN/HOLD status badge, which does not reliably predict debit eligibility",
      "detection": {
        "type": "text_present",
        "value": "Source share is HOLD and cannot be debited.",
        "scope": "page"
      }
    }
  ],
  "guardrails": {
    "allowlist_routes": [
      "/signon",
      "/menu",
      "/members",
      "/members/*"
    ],
    "allowed_action_types": [
      "select",
      "type",
      "click",
      "extract"
    ],
    "requires_confirmation": true
  },
  "evidence_policy": {
    "screenshot_on": [
      "checkpoint_failure",
      "hard_failure"
    ],
    "redact_fields": [
      "password",
      "E-mail",
      "Phone",
      "Address",
      "Share ID"
    ]
  },
  "escalation_policy": {
    "default": {
      "on_checkpoint_failure": {
        "retry": 2,
        "then": "escalate"
      },
      "on_unrecognized_dialog": "escalate",
      "on_step_timeout": {
        "retry": 1,
        "then": "escalate"
      },
      "on_hard_failure": "escalate"
    }
  }
}
